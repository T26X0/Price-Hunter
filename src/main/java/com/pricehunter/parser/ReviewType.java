package com.pricehunter.parser;

/** Причина, по которой автоматический импорт остановлен и требуется решение человека. */
public enum ReviewType {
    STORE_MATCH,
    PRODUCT_MATCH,
    CATEGORY_CLASSIFICATION
}
